int numOfLevels = 1;

void setup() {
  size(500, 500);
}

void draw() {
  background(255);
  drawTriangle();
w
  triangle(width/2, 0, 0, height, width, height);
  float newX1 = (0.5)*(1);
  float newY1;
  float newX2;
  float newY2;
  float newX3;
  float newY3;
  triangle(width/2, 0, 0, height, width, height);
}

void drawTriangle()
{
  fill(0);
  triangle(width/2, 0, 0, height, width, height);
}